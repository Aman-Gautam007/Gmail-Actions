# Gmail-Actions
This script checks whether an Apple Reminders item is completed. Only then can it move every non-trashed Gmail message carrying the `Advertisement` label to Trash. It never permanently deletes messages.
